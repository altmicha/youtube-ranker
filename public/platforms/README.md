# Platform cover images

Drop cover images here for the two homepage platform tiles. Same
fallback behavior as /public/categories — a missing file just shows
that platform's colored gradient instead.

- youtube.jpg
- twitch.jpg

Recommended aspect ratio: 3:4 (portrait) — cards render at a fixed
144×192px with `object-cover`.
